/**
 * Override existing configs or set new configs for a specific environment.
 * if NODE_ENV is set, the configurations here will override other similar settings
 */

 module.exports = {
 	
 	database: {

 	}
 	
 }