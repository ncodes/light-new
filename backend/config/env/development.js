/**
 * Override existing configs files or set new configs for a specific environment.
 * if NODE_ENV is set, the configurations here will override other similar settings.
 */

 module.exports = {
 	
 	// override settings in config/database.js
 	database: {
 		
 	},

 	// override settings in config/session.js
 	session: {
 		
 	}
 }